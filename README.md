Файл "newText" состоит из строк вида: "<host>" "<ip>" "<page>", где host — корневой домен, ip — IP-адрес, page — «хвост» ссылки.

### Задача

Необходимо вывести 5 IP-адресов, которые встречаются в файле чаще других.
